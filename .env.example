# Django settings
SECRET_KEY=your-secret-key-here-generate-a-real-one
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1

# Root user (created on first startup)
ROOT_USERNAME=root
ROOT_PASSWORD=changeme

# Production only
# CSRF_TRUSTED_ORIGINS=https://your-domain.com
